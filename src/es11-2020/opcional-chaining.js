// Optional chaining (?.)

const user = {};
console.log(user?.profile?.email);

// si no existe te devuelve undefined sin romper tu codigo.

// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining
