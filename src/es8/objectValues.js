// Object.values

// Trae un arreglos con los valores del objeto
const data = {
  frontend: "Oscar",
  backend: "Isabel",
  design: "Ana",
};

const values = Object.values(data);
console.log(values);
